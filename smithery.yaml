name: stresszero-mcp
description: "Qualitative workload reflection and StressZero public frame — no scoring, diagnosis, prediction or triage"
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    () => ({
      command: "npx",
      args: ["-y", "stresszero-mcp"]
    })
